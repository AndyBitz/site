{
  "name": "site",
  "version": "1.0.0",
  "scripts": {
    "build": "next build && next export && rm -Rf ./site && mv ./out ./site",
    "dev": "next"
  },
  "repository": "AndyBitz/site",
  "author": "AndyBitz",
  "license": "MIT",
  "dependencies": {
    "next": "^4.1.3",
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-motion": "^0.5.0"
  },
  "devDependencies": {
    "babel-plugin-inline-react-svg": "^0.4.0"
  },
  "babel": {
    "presets": [
      "next/babel"
    ],
    "plugins": [
      "inline-react-svg"
    ]
  }
}
